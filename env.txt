DISCORD_TOKEN = "discord bot key"
### 4. **Admin Commands**

| Command | Description | Example |
|---------|-------------|---------|
| `!setup_channels` | Set up registration panels | `!setup_channels` |
| `!generate_key "Product" 30` | Generate a product key | `!generate_key "Premium Cheat" 30` |
| `!check_account @user` | Check user's account | `!check_account @John` |
| `!stats` | Show system statistics | `!stats` |
| `!get_roles` | List all role IDs | `!get_roles` |

### 5. **User Workflow**

1. **User clicks "Register"** → Enters email/password
2. **User purchases your product** → You manually DM them a key
3. **User clicks "Redeem Key"** → Enters the key you sent
4. **Bot assigns Customer role** → User gets access

### 6. **How to Generate & Send Keys**
```
1. Customer buys product from you
2. You run: !generate_key "Premium Loader" 30
3. Bot creates key like: PREM-A3F2B8C1-9D4E7F2A
4. You DM the customer this key
5. Customer redeems it in Discord
